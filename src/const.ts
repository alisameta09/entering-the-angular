const baseApiUrl = 'https://icherniakov.ru/yt-course/';
const authUrl = `${baseApiUrl}auth/`;
const chatUrl = `${baseApiUrl}chat/`;
const messageUrl = `${baseApiUrl}message/`;

export {
  baseApiUrl,
  authUrl,
  chatUrl,
  messageUrl
}
